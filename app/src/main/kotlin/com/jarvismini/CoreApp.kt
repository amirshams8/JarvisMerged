package com.jarvismini

import android.app.Application

class CoreApp : Application() {

    override fun onCreate() {
        super.onCreate()

        // Engine wiring will be added later
        // Keeping app startup clean for now
    }
}
